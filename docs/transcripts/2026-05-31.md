# TopoCrafter Session — 2026-05-31

## Windows Build Fix (winCodeSign + NSIS)

Continued from previous session where `npm run build:win` was failing due to the
`winCodeSign-2.6.0.7z` archive containing macOS symlinks that Windows cannot create
without elevated privileges.

**Resolution:** User enabled Windows Developer Mode (Settings → System → For Developers).
This grants the `SeCreateSymbolicLinkPrivilege` right to normal user processes. The build
ran, winCodeSign extracted successfully, and the cache is now populated for future builds.
Developer Mode can be turned off again after the first successful build.

**NSIS bug fixed during build:** The `installer.nsh` macro used `MB_ICONWARNING` in a
`MessageBox` call. NSIS's parser only recognises `MB_ICONEXCLAMATION` (the Win32 name);
`MB_ICONWARNING` is a Windows API alias that NSIS doesn't expose. Fixed and rebuilt.

---

## EXR Heightmap Support — Sharp Replaced with hdrify

### Problem
Sharp 0.34.5 (used in previous session for EXR decoding via the main process) does not
include OpenEXR support in its pre-built Windows binaries. Any attempt to load an EXR
file through `sharp().metadata()` raised `Error: Input file contains unsupported image
format`.

### Investigation
- Confirmed that `@loaders.gl/exr` does not exist on npm (404).
- `openexr-js`, `exr-parser`, `node-openexr` also don't exist.
- User suggested the `hdrify` package.
- `npm info hdrify` confirmed: MIT, pure JS (deps: `buffer`, `fflate`, `jpeg-js`),
  supports EXR + HDR/RGBE, works in Node and browser. No native code.

### Solution
Replaced the entire sharp/IPC pipeline with `hdrify` running directly in the renderer.

**Files changed:**
- `src/renderer/src/utils/heightmap.ts` — removed `parseHeightmapExr(filePath)` IPC
  approach; new `parseHeightmapExr(buffer)` calls `readExr(buffer)` from hdrify, receives
  RGBA `Float32Array`, computes BT.709 luminance per pixel, normalises to `[0, 1]`.
- `src/main/index.ts` — removed `import sharp` and the `heightmap:decodeExr` IPC handler.
- `src/preload/index.ts` — removed `decodeExr` from the context bridge API.
- `src/renderer/src/types/index.ts` — removed `decodeExr` from `window.electronAPI`
  declaration.
- `package.json` — removed `sharp`, added `hdrify ^0.12.1`.
- `build/installer.nsh` — deleted (VC++ redistributable was only needed because sharp
  requires the MSVC runtime; hdrify has no native deps).
- `package.json` (build config) — removed `"include": "build/installer.nsh"` from NSIS
  section.

**Commit:** `7af9684`

---

## Hillshade Tab — Controls Disabled Off-Tab

**Request:** All hillshade parameter controls (Sun Azimuth, Sun Altitude, Vertical
Exaggeration, Shadow Depth, Brightness) should be disabled when the active tab is not
Hillshade, and re-enabled when switching back.

**Implementation** (`src/renderer/src/components/ParameterPanel/ParameterPanel.tsx`):
- Added `const activeTab = useStore((s) => s.activeTab)`.
- Derived `const hillshadeDisabled = activeTab !== 'hillshade'`.
- Added `disabled={hillshadeDisabled}` to all five Mantine `Slider` components in the
  hillshade section. The "Actual Z Factor" `NumberInput` was already disabled (read-only).

---

## Sticky Tabs — Prevent Scrolling Off Screen

**Problem:** When the map image was scrolled vertically, the Terrain/Hillshade tab bar
would scroll off the top of the viewport.

**Root cause (final analysis):** `height: '100%'` on `AppShell.Main` referenced the
parent AppShell root's `min-height: 100dvh`, giving Main a computed height of `100dvh`.
But Main starts at y=52 (after the sticky header in the flex column), so it extended to
y=52+100dvh — below the viewport — causing body-level scroll instead of canvas-level
scroll. The tabs scrolled away with the body.

**Attempts:**
1. `overflow: 'hidden'` on `AppShell.Main` — no effect because Main's height still
   overflowed (height > viewport, so there was still a scroll container above it).
2. `position: 'sticky', top: 52, zIndex: 100` on the `Tabs` component — partially worked
   (tabs stayed visible) but caused the horizontal scrollbar issue described below.

**Final fix** (`src/renderer/src/App.tsx`):
- Removed `height: '100%'` from `AppShell.Main` style — now `flex: 1` from Mantine's
  CSS correctly sizes Main to `100dvh − 52px`.
- Added `overflow: 'hidden'` to `AppShell.Main` — body no longer scrolls.
- Removed `position: sticky` from the Tabs (no longer needed; tabs are always visible
  because body scroll is gone).

---

## Zoom Slider

**Request:** Add a zoom in/out slider to the toolbar, left of the Export button. Center
position = 100% (normal), left = zoom out, right = zoom in.

### Store (`src/renderer/src/types/index.ts`, `src/renderer/src/store/useStore.ts`)
- Added `mapZoom: number` (default `100`) to `ProjectState` and `initialState`.
- Added `setMapZoom: (zoom: number) => void` action.

### Toolbar (`src/renderer/src/components/Toolbar/Toolbar.tsx`)
- Imported `Slider` from Mantine.
- Added a `Group` containing a 120px-wide `Slider` and a percentage `Text` label,
  inserted into the right-side group before the Export menu.
- **Scale:** exponential symmetric mapping so that the visual center = 100%:
  - Slider internal range: −100 to 100, step 5.
  - `sliderToZoom(v) = Math.round(100 × 2^(v/50))` → −100 → 25%, 0 → 100%, 100 → 400%.
  - `zoomToSlider(z) = Math.round(50 × log₂(z/100))` for the controlled value.
- Double-clicking the percentage label resets zoom to 100%.
- Slider tooltip shows the computed zoom% while dragging.

### MapCanvas (`src/renderer/src/components/MapCanvas/MapCanvas.tsx`)
- Subscribes to `mapZoom`.
- Wraps the image and both SVG overlay layers in a `display: inline-block` inner div
  with `width: ${mapZoom}%`. The image uses `width: 100%` to fill the inner div.
  SVGs use `position: absolute, width: 100%, height: 100%` relative to the inner div.
- At zoom > 100% the inner div overflows the outer scroll container → horizontal
  scrollbar appears. At zoom < 100% the image shrinks.

---

## Horizontal Scrollbar — Fixed at Viewport Bottom

**Problem:** When the image was zoomed in (horizontal overflow), scrolling the image
vertically caused the horizontal scrollbar to disappear off the bottom of the screen.

**Root cause:** Even after removing `height: '100%'` from AppShell.Main, body scroll was
still possible in some configurations. The MapCanvas outer div needed `minHeight: 0` to
prevent the flex item from growing to fit its content — without it, `flex: 1` on the div
allows it to expand beyond the container, disabling the scroll container behaviour.

**Fix** (`src/renderer/src/components/MapCanvas/MapCanvas.tsx`):
- Added `minHeight: 0` to the outer div. Combined with `overflow: auto` and `flex: 1`,
  the div is now a true bounded scroll container. Both horizontal and vertical scrollbars
  are anchored within the canvas area and never scroll off screen.

---

## Commits This Session

| Hash | Description |
|------|-------------|
| `7af9684` | Replace sharp/IPC EXR decoder with hdrify; fix sticky tabs; disable hillshade controls off-tab |
| *(pending)* | Zoom slider, horizontal scrollbar fix |
